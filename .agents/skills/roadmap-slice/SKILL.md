---
name: roadmap-slice
description: Implement the next poker-range-trainer roadmap slice (or a specific one via a slice number), validate it, commit and push it, and queue the prompt for the slice after it. Use when the user wants to advance docs/roadmap.md one small, committed slice at a time.
---

# roadmap-slice

The full procedure lives in `.claude/skills/roadmap-slice/SKILL.md`.
Read that file and follow it as written. It is the single source of truth for this
skill; this file only makes the skill discoverable to Codex.

Project rules referenced there as `CLAUDE.md` are the same rules `AGENTS.md` points to.
