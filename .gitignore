# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test coverage output
coverage

# Environment variables (keep real env files out; allow the committed template)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP scratch output (snapshots, console logs)
.playwright-mcp

# Screenshots dropped at the repo root by browser tooling given a bare filename.
# Root-level only: real image assets live in mobile/assets/.
/*.png

# App Store screenshot candidates: the PNGs are regenerable upload assets, not
# source. The seed backup that reproduces the on-screen data IS tracked, so a
# re-shoot starts from the same library.
/screenshots/*.png
